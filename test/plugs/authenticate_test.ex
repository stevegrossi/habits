defmodule Habits.Plugs.AuthenticateTest do
  use MyApp.ConnCase

  test "without a current_account finds one in the database"
  test ""
  # assert redirected_to(conn) == "/sign_in"
  # assert conn.status != 302
end
